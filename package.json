{
  "name": "brackenvale",
  "version": "0.0.0",
  "description": "Brackenvale — a top-down pixel-art action RPG (TypeScript + Vite + Phaser)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
